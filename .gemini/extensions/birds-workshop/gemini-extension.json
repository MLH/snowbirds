{
  "name": "birds-workshop",
  "version": "1.0.0",
  "description": "Interactive bird drawing workshop for MLH AI Roadshow. Guides attendees through drawing a bird, cleaning it up with AI, generating a flight animation, and pushing it to the live display.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "birds": {
      "command": "node",
      "args": ["${extensionPath}${/}mcp-server${/}server.js"],
      "cwd": "${extensionPath}${/}mcp-server",
      "env": {
        "GOOGLE_GEMINI_API_KEY": "${env:GOOGLE_GEMINI_API_KEY}",
        "PROJECT_DIR": "${workspacePath}"
      }
    }
  }
}
